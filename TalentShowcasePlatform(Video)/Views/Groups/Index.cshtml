@model IEnumerable<TalentShowcasePlatform.Models.Group>

<h2>Groups</h2>

<a href="/Groups/Create" class="btn btn-primary">Create New Group</a>

<ul>
    @foreach (var g in Model)
    {
        <li>
            <a href="/Groups/Details/@g.Id">@g.Name</a>
        </li>
    }
</ul>
